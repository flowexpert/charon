# Install folder of charon-core (or source directory)
charon-core-install	@CHARON_CORE_ROOT_DIR@

# compiler command for plugin generation
compiler-call		@CMAKE_CXX_COMPILER@ @CMAKE_CXX_FLAGS@ @CMAKE_CXX_FLAGS_RELEASE@

# Unix specific folders
x11-libs			@X11_LIBRARIES@

# Windows specific folders
sdk-root			@SDK_ROOT@
vc-root				@VC_ROOT@
