INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})

SET(MYLIB_SRC
	MyTabWidget.cpp
	MyTabWidget.h
)

QT4_AUTOMOC(${MYLIB_SRC})

ADD_LIBRARY(mylib ${MYLIB_SRC})
TARGET_LINK_LIBRARIES(mylib ${QT_LIBRARIES})
