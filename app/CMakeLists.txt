INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)

SET(APP_SRC
	main.cpp
	mainwindow.cpp
	mainwindow.h
)

QT4_AUTOMOC(${APP_SRC})

ADD_EXECUTABLE(templategenerator ${APP_SRC})
TARGET_LINK_LIBRARIES(templategenerator
	mylib
	${QT_LIBRARIES}
)
